  <header class="site-header">
    <div class="wrap">
      <a class="brand" href="{{ brandHref | default('#top') }}">
        <img class="brand-mark" src="{{ rootPath | default('') }}images/brand/app-icon-128.png" width="28" height="28" alt="Climbable logo">
        <span class="brand-name">Climbable</span>
      </a>
      <nav class="header-nav" aria-label="Section navigation">
        <a href="{{ navPrefix | default('') }}#problem">Problem</a>
        <a href="{{ navPrefix | default('') }}#how-it-works">How it works</a>
        <a href="{{ navPrefix | default('') }}#scores">What it scores</a>
        <a href="{{ navPrefix | default('') }}#coverage">Coverage</a>
        <a href="{{ navPrefix | default('') }}#pricing">Pricing</a>
      </nav>
      <div class="header-actions">
        <button class="theme-toggle" data-theme-toggle aria-label="Toggle dark mode">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        </button>
        <a class="btn btn-primary" href="/webapp/">Open app</a>
      </div>
    </div>
  </header>
